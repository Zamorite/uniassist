class Task {
  final String id, content, ownerId, type, status;
  final DateTime deadline;

  Task({
    this.id,
    this.content,
    this.ownerId,
    this.type,
    this.deadline,
    this.status,
  });
}
